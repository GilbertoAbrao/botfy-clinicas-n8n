# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database Configuration
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# N8N Configuration
NEXT_PUBLIC_N8N_URL=https://your-n8n-instance.easypanel.host

# N8N Webhook URLs for Calendar Integration
# These webhooks need to be created in N8N to receive calendar events
# See AGENTS.md for detailed setup instructions

# Triggered when appointment is created manually via calendar
N8N_WEBHOOK_APPOINTMENT_CREATED=https://your-n8n-instance.easypanel.host/webhook/calendar/appointment-created

# Triggered when appointment is updated (time changed, status changed)
N8N_WEBHOOK_APPOINTMENT_UPDATED=https://your-n8n-instance.easypanel.host/webhook/calendar/appointment-updated

# Triggered when appointment is cancelled
N8N_WEBHOOK_APPOINTMENT_CANCELLED=https://your-n8n-instance.easypanel.host/webhook/calendar/appointment-cancelled

# Triggered when waitlist patient should be notified
N8N_WEBHOOK_WAITLIST_NOTIFY=https://your-n8n-instance.easypanel.host/webhook/calendar/waitlist-notify

# Triggered when pre-checkin reminder needs to be sent
N8N_WEBHOOK_PRE_CHECKIN_REMINDER=https://your-n8n-instance.easypanel.host/webhook/pre-checkin/reminder

# Evolution API (WhatsApp Gateway)
EVOLUTION_API_URL=https://your-evolution-api.easypanel.host
EVOLUTION_INSTANCE=Your Instance Name
